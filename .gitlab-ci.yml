image: node:latest
pages:
  stage: deploy
  script:
    - npm install --legacy-peer-deps # 解决 React 19 冲突
    - npm run build
    - rm -rf public
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - main